/**
 * Site-wide identity copy.
 *
 * Kept isolated from components so the brand name and the (currently
 * placeholder) tagline can be edited in one place without touching layout
 * or page code.
 */
export const siteConfig = {
  name: "tinyutility",
  /**
   * TEMPORARY placeholder description — replace when final copy is ready.
   */
  tagline: "Five projects, built along the way.",
  description:
    "TinyUtility Hub is a living collection of websites, tools, and experiments built under TinyUtility.",
  url: "https://hub.tinyutility.space",
};
